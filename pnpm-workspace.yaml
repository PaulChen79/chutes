# esbuild's postinstall only fetches a platform binary that pnpm already installs
# as an optional dependency, so the script is denied rather than approved.
# Without an explicit decision here, `pnpm install` fails with ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  esbuild: false
